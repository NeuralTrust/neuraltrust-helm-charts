# NeuralTrust Data Plane Environment Variables
# Copy this file to .env.data-plane.dev or .env.data-plane.prod and fill in the values

# =============================================================================
# MANDATORY SECRETS
# =============================================================================

# At least ONE of the following API keys is REQUIRED:
# - OPENAI_API_KEY (for OpenAI models)
# - GOOGLE_API_KEY (for Google/Gemini models)

# OpenAI API Key
# Required if using OpenAI models
# This defines the default LLM model to use
# Note: You can configure different LLM providers through the WebUI
OPENAI_API_KEY=""

# Google API Key
# Required if using Google/Gemini models
# This defines the default LLM model to use
# Note: You can configure different LLM providers through the WebUI
GOOGLE_API_KEY=""

# HuggingFace Token
# Required: Token for accessing HuggingFace models
# This token should have been provided by NeuralTrust
# If not provided, some features may not work properly
HUGGINGFACE_TOKEN=""

# =============================================================================
# OPTIONAL SECRETS
# =============================================================================

# Data Plane JWT Secret
# Optional: JWT secret for data plane authentication
# If not provided, a new one will be automatically generated
# This secret should match the CONTROL_PLANE_JWT_SECRET in control plane
DATA_PLANE_JWT_SECRET=""

# Resend API Key
# Optional: Used for sending emails
RESEND_API_KEY=""

# ClickHouse Backup - S3 Configuration
# Optional: S3 credentials for ClickHouse backups
# Only needed if enabling ClickHouse backup with S3 storage
S3_ACCESS_KEY=""
S3_SECRET_KEY=""

# ClickHouse Backup - GCS Configuration
# Optional: Google Cloud Storage credentials for ClickHouse backups
# Only needed if enabling ClickHouse backup with GCS storage
GCS_ACCESS_KEY=""
GCS_SECRET_KEY=""

# GCR Key File Path
# Optional: Path to Google Container Registry service account key (JSON format)
# Required if using NeuralTrust private registry (default behavior)
# This key is provided by NeuralTrust to pull images from GCR
# Example: GCR_KEY_FILE="/path/to/gcr-key.json"
GCR_KEY_FILE="./gcr-keys.json"
