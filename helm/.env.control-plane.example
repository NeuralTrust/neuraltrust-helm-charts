# NeuralTrust Control Plane Environment Variables
# Copy this file to .env.control-plane.dev or .env.control-plane.prod and fill in the values

# =============================================================================
# MANDATORY SECRETS
# =============================================================================

# Control Plane JWT Secret
# This MUST be the same JWT secret used by your data plane installation.
# To get the data plane JWT secret, run:
#   kubectl get secret data-plane-jwt-secret -n neuraltrust -o jsonpath='{.data.DATA_PLANE_JWT_SECRET}' | base64 --decode
CONTROL_PLANE_JWT_SECRET=""

# PostgreSQL Password
# Required for PostgreSQL database access
# If installing PostgreSQL in cluster (--install-postgresql), this will be used for the in-cluster database
# If using external PostgreSQL, this should match your external database password
POSTGRES_PASSWORD=""

# Data Plane API URL
# The URL of the Data Plane API service
# If deploying in the same cluster, use:
#   DATA_PLANE_API_URL="http://data-plane-api-service.neuraltrust.svc.cluster.local"
# For external access, use your ingress URL:
#   DATA_PLANE_API_URL="https://data-api.example.com"
DATA_PLANE_API_URL=""

# OpenAI API Key
# Required for LLM functionality in the control plane
# This defines the default LLM model to use
OPENAI_API_KEY=""

# =============================================================================
# OPTIONAL SECRETS
# =============================================================================

# PostgreSQL Host (only if using external PostgreSQL, not installing in cluster)
# Leave empty if using --install-postgresql flag
# Example: POSTGRES_HOST="postgres.example.com"
POSTGRES_HOST=""

# Resend API Key
# Optional: Used for sending emails (alerts, invites, etc.)
RESEND_API_KEY=""

# Resend Alert Sender Email
# Optional: Email address used as sender for alert emails
RESEND_ALERT_SENDER=""

# Resend Invite Sender Email
# Optional: Email address used as sender for invite emails
RESEND_INVITE_SENDER=""

# Trustgate JWT Secret
# Optional: JWT secret for Trustgate integration
TRUSTGATE_JWT_SECRET=""

# Firewall JWT Secret
# Optional: JWT secret for Firewall integration
FIREWALL_JWT_SECRET=""

# Model Scanner Secret
# Optional: Secret for Model Scanner integration
MODEL_SCANNER_SECRET=""

# GCR Key File Path
# Optional: Path to Google Container Registry service account key (JSON format)
# Required if using NeuralTrust private registry (default behavior)
# This key is provided by NeuralTrust to pull images from GCR
# Example: GCR_KEY_FILE="/path/to/gcr-key.json"
GCR_KEY_FILE="./gcr-keys.json"
